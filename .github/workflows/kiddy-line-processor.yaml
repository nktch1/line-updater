name: main-workflow
on:
  push:
    branches:
      - master

jobs:
  Hello:
    runs-on: alpine3.11
    steps:
      - name: test
        run: go test -v -race -timeout 30s ./...
      - name: build
        run: go build -v ./cmd/kiddy-line-processor